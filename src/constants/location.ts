/**
 * Fallback location until expo-location is wired up (follow-up).
 * Berlin — the developer's city; the Settings screen will replace this.
 */
export const DEFAULT_LOCATION = {
  lat: 52.52,
  lng: 13.405,
  label: 'Berlin',
} as const;